#include "main.h"
/**
 * _sqrt_recursion - prints natural square root of a number
 * @n: number whose square is to be determined
 * Return: result and -1 when n doesnt have a natural square root
*/
int _sqrt_recursion(int n)
{
	int result;

	if (result != _sqrt_recursion(n))
		return (-1);
	else if
		(result = _sqrt_recursion(n));
	return (result);
}
