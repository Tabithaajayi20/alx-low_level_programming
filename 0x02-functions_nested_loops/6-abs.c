#include "main.h"

/**
 *  _abs - computes absolute value of an integer
 * @n: the integer in question
 * Return: absolute of the integer
*/

int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
